{
  "services": {
    "web": {
      "root": ".",
      "framework": "nextjs",
      "buildCommand": "keystone build --no-ui && prisma migrate deploy && next build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": {
        "type": "service",
        "service": "web"
      }
    }
  ]
}
